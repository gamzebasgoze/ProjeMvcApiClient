@using ProjeMvcApiClient.Models;
@model List<Depolar>
@{
    Layout = null;
}
<html>
<head>
    <title></title>
    <link href="~/twitter-bootstrap/css/bootstrap.css" rel="stylesheet" />
    <link href="~/twitter-bootstrap/css/bootstrap-grid.css" rel="stylesheet" />
</head>
<body>
    <div class="container mt-2">
        <div class="bg-dark text-white-50 lead text-center my-2 p-2">
            Depoların Listesi
        </div>
        <div class="text-right pr-2 my-2">
            <a asp-action="Add" asp-controller="Depolar" class="btn btn-primary btn-sm">EKLE</a>
        </div>
        <table class="table table-hover table-striped table-bordered table-sm">
            <tr>
                <th>@Html.Label("Depo Id")</th>
                <th>@Html.Label("Depo Adı")</th>
                <th>@Html.Label("Depo Bölge")</th>
            </tr>
            @foreach (var depolar in Model)
            {
                <tr>
                    <td>@depolar.DepoId</td>
                    <td>@depolar.DepoAdi</td>
                    <td>@depolar.DepoBolge</td>
                    <td>
                        <a asp-action="Edit" asp-controller="Depolar" asp-route-id="@depolar.DepoId" class="btn btn-primary btn-sm">Güncelle</a>
                        <a onclick="return confirm('Silmek istediğinizden emin misiniz?')" asp-action="Delete" asp-controller="Depolar" asp-route-id="@depolar.DepoId" class="btn btn-warning btn-sm">Sil</a>
                    </td>
                </tr>
            }
        </table>
    </div>
</body>
</html>